# 📁 Структура проекта

## Обзор

Проект организован в модульную структуру для удобства разработки и поддержки.

```
RecSys31/
├── src/                          # Исходный код
│   ├── loaders/                  # Модули загрузки данных
│   │   ├── __init__.py
│   │   └── data_loader.py        # Загрузка Parquet файлов
│   ├── preprocessing/            # Модули предобработки
│   │   ├── __init__.py
│   │   └── preprocessing.py      # Объединение событий с товарами
│   ├── features/                 # Feature engineering
│   │   ├── __init__.py
│   │   └── feature_engineering.py # Создание признаков пользователей
│   └── portraits/                # Генерация портретов
│       ├── __init__.py
│       └── user_portrait.py     # Создание и вывод портретов
│
├── scripts/                      # Скрипты запуска
│   └── main.py                   # Главный скрипт генерации портрета
│
├── data/                         # Данные и конфигурация
│   ├── t_ecd_data/              # Исходные данные (Parquet)
│   │   └── dataset/
│   │       └── small/
│   ├── notebooks/               # Jupyter notebooks для анализа
│   │   ├── eda.ipynb
│   │   └── eda2.ipynb
│   └── README.md                # Документация по данным
│
├── output/                       # Результаты работы
│   ├── preprocessed/            # Предобработанные данные
│   └── user_portrait_*.json     # Сгенерированные портреты
│
├── config/                       # Конфигурационные файлы (если нужны)
│
└── README.md                     # Основная документация проекта
```

## Использование

### Запуск из корня проекта

```bash
# Из корня проекта RecSys31/
python3 scripts/main.py --user_id 25770580

# С параметрами оптимизации
python3 scripts/main.py --user_id 25770580 --max_files 10 --sample_ratio 5
```

### Пути по умолчанию

- **Данные**: `./data/t_ecd_data/dataset/small`
- **Вывод**: `./output/user_portrait_{user_id}.json`

## Импорты

Все модули доступны через пакет `src`:

```python
from src.loaders import load_user_events, load_reference_data
from src.preprocessing import merge_events_with_items
from src.features import create_user_features
from src.portraits import create_user_portrait_from_features, print_user_portrait
```

## Преимущества структуры

1. **Модульность**: Каждый компонент в отдельной папке
2. **Масштабируемость**: Легко добавлять новые модули
3. **Чистота**: Исходный код отделен от данных и результатов
4. **Переиспользование**: Модули можно импортировать в других проектах

